{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build avec make",
      "type": "shell",
      "command": "make re",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": "build",
      "problemMatcher": "$gcc"
    }
  ]
}